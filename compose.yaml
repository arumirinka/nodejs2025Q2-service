version: "v2.35.1-desktop.1"
services:
  api: 
    build: .
    container_name: api
    ports:
      - '4000:4000'
    restart: always
    volumes:
      - ./src:/app/src
      - /app/node_modules
    networks:
      - is-ls

networks:
  is-ls:
    driver: bridge